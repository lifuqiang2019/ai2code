/**
 * 重新导出 @voyager/editor 的类型和类
 * 这样 UI 组件可以使用 import { DesignEditor } from "../editor"
 */

export type {
  DesignEditor,
  DesignValue,
  ElementDef,
  ShapeDef,
  ShapePath,
  ShapeViewBox,
  ShapeStroke,
  ShapeFill,
  ID,
  Selection,
} from "@voyager/editor";

